Amazon Web Scraper

This app pulls the product's name and price from Amazon.com using product's URL, and displays it on a webpage using Flask. 

Installs:
- requires python with pip
- pip install -r requirements.txt

Prerequisites:
- make sure links.txt contains compatible amazon url(s). 
- Product page must have HTML element of 'corePriceDisplay_desktop_feature_div' as an id.

To run:
- python index.py
